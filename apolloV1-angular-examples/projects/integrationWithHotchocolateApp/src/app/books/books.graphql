query BooksQuery {
    books {
      nodes {
        id
        title
      }
    }
  }

  query BooksQueryAllSimpleFields {
    books {
      nodes {
        id
        title
        price
        authorId,
        timeStamp
      }
    }
  }
